{
  "name": "amplify-swift-ios-repros",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "scripts": {
    "sandbox": "npx ampx sandbox",
    "generate:outputs": "npx ampx generate outputs --format json --out-dir .",
    "xcodegen": "cd ios && xcodegen generate"
  },
  "devDependencies": {
    "@aws-amplify/backend": "^1.16.1",
    "@aws-amplify/backend-cli": "^1.8.0",
    "@types/node": "^24.0.3",
    "aws-cdk": "^2.142.1",
    "aws-cdk-lib": "^2.142.1",
    "constructs": "^10.3.0",
    "tsx": "^4.10.5",
    "typescript": "^5.8.3"
  }
}
